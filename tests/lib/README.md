# Tests for the Standard Library

Currently snapshots. The tests for library `foo` are called `libFoo` to not spam
your editor's fuzzy finder, because most of the time you will want to go to the
library definition of `foo`, not these tests.