/* commands.h -- the commands of urt, one function each.  Each takes the
 * arguments after its name, argv[0] being the name, and returns the exit
 * status. */

#ifndef URT_COMMANDS_H
#define URT_COMMANDS_H

int snapshot_main(int argc, char **argv);

#endif
